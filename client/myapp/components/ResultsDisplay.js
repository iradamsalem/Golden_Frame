import React from 'react';
import { View, Text, Image, StyleSheet, TouchableOpacity } from 'react-native';

const ResultsDisplay = ({ selectedPhoto, purpose, onReset }) => {
  if (!selectedPhoto || !purpose) return null;

  return (
    <View style={styles.container}>
      <Text style={styles.title}>Your Perfect Photo</Text>

      <View style={styles.row}>
        <View style={styles.imageWrapper}>
          <Image
            source={{ uri: selectedPhoto.uri }}
            style={styles.image}
            resizeMode="contain"
          />
        </View>

        <View style={styles.feedbackWrapper}>
          <View style={styles.card}>
            <Text style={styles.cardTitle}>
              Why This Works for {purpose.charAt(0).toUpperCase() + purpose.slice(1)}
            </Text>

            {/* במקום הפידבק הקבוע, טקסט זמני */}
            <Text style={styles.cardText}>
              Feedback will appear here once it's generated by our AI engine.
            </Text>
          </View>

          <TouchableOpacity style={styles.button} onPress={onReset}>
            <Text style={styles.buttonText}>Try Again with New Photos</Text>
          </TouchableOpacity>
        </View>
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 20,
    backgroundColor: '#0a0a23',
    borderRadius: 10,
  },
  title: {
    fontSize: 26,
    color: 'gold',
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 20,
  },
  row: {
    flexDirection: 'column',
  },
  imageWrapper: {
    marginBottom: 20,
    backgroundColor: '#1a1a2e',
    padding: 8,
    borderRadius: 10,
  },
  image: {
    width: '100%',
    height: 300,
    borderRadius: 10,
  },
  feedbackWrapper: {
    gap: 12,
  },
  card: {
    backgroundColor: '#2b2b3c',
    padding: 16,
    borderRadius: 10,
  },
  cardTitle: {
    fontSize: 18,
    color: 'gold',
    fontWeight: '600',
    marginBottom: 8,
  },
  cardText: {
    color: '#ddd',
    fontSize: 14,
  },
  button: {
    marginTop: 20,
    alignSelf: 'center',
    backgroundColor: 'gold',
    paddingVertical: 10,
    paddingHorizontal: 20,
    borderRadius: 25,
  },
  buttonText: {
    color: '#1a1a2e',
    fontWeight: 'bold',
    fontSize: 16,
  },
});

export default ResultsDisplay;
